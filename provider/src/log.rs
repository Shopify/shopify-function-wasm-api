use crate::decorate_for_target;

decorate_for_target! {
    /// The most significant 32 bits are the result, the least significant 32 bits are the pointer.
    fn shopify_function_log_new_utf8_str(_len: usize) -> usize {
        unimplemented!()
    }
}
